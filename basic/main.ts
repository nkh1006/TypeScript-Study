// compiling and variables

const bob = "hey I'am Bob !";
const a = 5.321;
interface MyObj {
    x: number;
    y: number;
};
const obj: MyObj = {
    x: 5,
    y: 7
};
const bob2 = () => {};
console.log(bob);

// functions

type AddFunc = (x: number, y: number) => string;

const add = (x, y) => x + y;

// unions

interface Dog {                     

    
}

// casting

// any